HISTORICAL PROMPT · DO NOT RUN

这份提示词对应的文档问题已在后续产品交接收口中处理。下一个 Codex 任务应等待产品侧决策后的新 handoff。下方内容仅作历史记录。

Syllab for NTU Learn 仓库。你之前在这个仓库工作过，git 里有的不用我讲。

在 `58dcf09` 之后有一次接手，负责 v0.1.0 的交付打包，没碰功能代码。它留下了三处未提交改动和一个未跟踪的交付包——
这些在 `git log` 里根本看不到，所以有一份交接专门报告这件事。

先做两步：

1. 跑 `git status`。
2. 读 `Syllab_MVP_Delivery_v0.1.0/02_CODEX_HANDOFF_v0.1.0.md`。第 1 节是那次接手完成的工作清单，
   第 2 节逐条说明它留下了什么、哪一项不是它留的，第 3 节说明它为什么重打了扩展包，
   第 5 节是它只读评估出的文档问题（一条都没改）。

然后停下来告诉我三件事：你理解的当前状态、工作树里有没有真异常、以及你打算做什么。我确认前不要改任何文件。

如果这一轮要动第 5 节列的那些文档问题，先说一声再动——上游那几份是产品侧的设计记录。

这一轮的任务：[在这里写你这一轮要做的事]
